import tkinter as tk

root = tk.Tk()
root.title("Digikids example 3")
root.geometry("600x400+50+50")
#resizable() has 2 parameters that specify the width and #height of the window, you specify if it resixable or not
root.resizable(False, False)
#root.attributes('-alpha', 0.5) #defining the transparency
#root.attributes('-topmost', 1) #defines the window stack order

root.mainloop()

















